<Page
    x:Class="HerdMe.Windows.Pages.UpdatesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    x:Uid="UpdatesPageRoot"
    AutomationProperties.AutomationId="UpdatesPageRoot"
    Loaded="Page_Loaded"
    Unloaded="Page_Unloaded">
    <Grid RowSpacing="8">
        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
        <ScrollViewer Grid.Row="1">
            <StackPanel Padding="{StaticResource PagePadding}" Spacing="20">
                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Spacing="4">
                        <TextBlock x:Uid="UpdatesTitle" Text="Updates" Style="{StaticResource PageTitleStyle}" />
                        <TextBlock x:Name="LastCheckedText" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                        <Button x:Name="RefreshButton" Click="Refresh_Click">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <SymbolIcon Symbol="Refresh" />
                                <TextBlock x:Uid="UpdatesRefreshButtonText" Text="Check" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="UpdateAllButton" Click="UpdateAll_Click" Style="{StaticResource AccentButtonStyle}" Visibility="Collapsed">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <SymbolIcon Symbol="Download" />
                                <TextBlock x:Uid="UpdatesUpdateAllButtonText" Text="Update all" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>

                <InfoBar
                    x:Name="StatusBar"
                    IsClosable="True"
                    IsOpen="False"
                    Severity="Informational" />

                <StackPanel Spacing="8">
                    <Grid>
                        <TextBlock x:Uid="DownloadsTitle" Text="Downloads" Style="{StaticResource SectionTitleStyle}" />
                        <Button HorizontalAlignment="Right" Click="ClearDownloads_Click" Style="{StaticResource ToolbarIconButtonStyle}">
                            <ToolTipService.ToolTip><ToolTip><TextBlock x:Uid="DownloadsClear" Text="Clear completed history" /></ToolTip></ToolTipService.ToolTip>
                            <SymbolIcon Symbol="Clear" />
                        </Button>
                    </Grid>
                    <StackPanel x:Name="DownloadRows" Spacing="8" />
                </StackPanel>

                <StackPanel Spacing="10">
                    <TextBlock Text="HerdMe" Style="{StaticResource SectionTitleStyle}" />
                    <Border
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="16">
                        <Grid ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Spacing="5">
                                <TextBlock Text="HerdMe" FontWeight="SemiBold" />
                                <TextBlock x:Name="ApplicationVersionText" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                <TextBlock x:Name="ApplicationStatusText" TextWrapping="Wrap" />
                            </StackPanel>
                            <Button
                                x:Name="DownloadApplicationButton"
                                Grid.Column="1"
                                Click="DownloadApplication_Click"
                                VerticalAlignment="Center"
                                Visibility="Collapsed"
                                Style="{StaticResource AccentButtonStyle}">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <SymbolIcon Symbol="Download" />
                                    <TextBlock x:Uid="UpdatesDownloadButtonText" Text="Download" />
                                </StackPanel>
                            </Button>
                        </Grid>
                    </Border>
                </StackPanel>

                <StackPanel Spacing="10">
                    <TextBlock x:Uid="UpdatesComponentsTitle" Text="Components and services" Style="{StaticResource SectionTitleStyle}" />
                    <ProgressBar x:Name="ComponentCheckProgress" IsIndeterminate="True" Visibility="Collapsed" />
                    <StackPanel x:Name="ComponentRows" Spacing="8" />
                    <Border
                        x:Name="EmptyState"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="20">
                        <StackPanel HorizontalAlignment="Center" Spacing="8">
                            <SymbolIcon Symbol="Accept" />
                            <TextBlock x:Uid="UpdatesNoComponents" Text="All installed components are up to date." TextAlignment="Center" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <Grid x:Name="BusyOverlay" Visibility="Collapsed" Padding="28,12,28,0">
            <StackPanel Orientation="Horizontal" Spacing="12">
                <ProgressRing Width="18" Height="18" IsActive="True" />
                <TextBlock x:Name="BusyText" TextAlignment="Center" TextWrapping="Wrap" MaxWidth="440" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
